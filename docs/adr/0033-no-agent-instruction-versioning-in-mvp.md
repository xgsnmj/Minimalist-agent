# No agent instruction versioning in the MVP

Minimalist Agent will let Administrators edit an Agent's instruction directly without draft, publish, rollback, or version history workflows in the MVP. Agent Runs should still record the Agent Instruction snapshot used at run start so debugging can explain behavior after an instruction changes. Full instruction versioning can be added later if change review, rollback, or reproducibility becomes a stronger requirement.
