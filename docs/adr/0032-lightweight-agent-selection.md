# Lightweight agent selection

Minimalist Agent will let Users choose from enabled Agents through a lightweight Agent Selection control when starting a new Agent Conversation. Existing conversations remain bound to the Agent used when they were created, and continuing a conversation uses that Agent by default. The MVP will show enough Agent identity for orientation, such as name and icon, but will not include an agent marketplace, expert catalog, ratings, or complex discovery taxonomy.
