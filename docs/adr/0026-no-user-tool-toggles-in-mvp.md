# No user tool toggles in the MVP

Minimalist Agent will not let Users enable or disable tool capabilities from the conversation composer in the MVP. Tool availability is governed by each Agent's capability policy, and an Agent Run records the resolved Run Capability Snapshot at start time. The UI can show which capabilities are available for transparency, but runtime capability changes remain an Administrator Console responsibility.
