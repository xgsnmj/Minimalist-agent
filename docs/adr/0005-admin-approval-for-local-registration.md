# Administrator approval for local registration

Minimalist Agent will require Administrator approval before a newly registered Local Account can use the platform. Agent capabilities can consume paid model tokens, reach external MCP servers, use sandboxes, and access search capability, so open self-service activation would create unnecessary cost and security exposure. The MVP will support pending, enabled, rejected, and disabled account states, with an initial Administrator bootstrapped outside normal registration.
