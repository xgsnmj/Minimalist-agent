# Ninety-day full trace retention

Minimalist Agent will retain Full Trace records for 90 days by default. This window is long enough for debugging, audit review, model behavior investigation, and capability policy tuning, while limiting unbounded object storage growth. Administrators can later receive storage usage views and manual cleanup controls, but the MVP will use 90 days as the platform default.
