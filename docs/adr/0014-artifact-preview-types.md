# Initial artifact preview types

Minimalist Agent will support Markdown, plain text, images, PDF, code files, JSON or table-like data, and sandboxed HTML as the initial Artifact Preview types. Office documents are downloadable in the MVP unless they are converted to PDF or another supported preview format. HTML artifacts must render inside a sandboxed iframe rather than being mounted into the main application DOM.
