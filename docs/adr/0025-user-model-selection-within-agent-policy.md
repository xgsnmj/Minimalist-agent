# User model selection within agent policy

Minimalist Agent will allow Users to choose a model for an Agent Run from the current Agent's Allowed Model Selection. Administrators configure each Agent's default Model Configuration and the enabled alternatives, while the conversation composer exposes only that governed set. Each Agent Run records the actual Model Configuration used so cost, behavior, and debugging can be traced back to the selected model.
