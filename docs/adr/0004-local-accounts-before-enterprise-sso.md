# Local accounts before enterprise SSO

Minimalist Agent will implement Local Accounts for the MVP and reserve an Enterprise SSO Boundary without shipping an SSO sign-in flow. Simple registration, login, administrator bootstrapping, and administrator-managed account access are enough for the first product version. Enterprise SSO would introduce identity provider setup, callback handling, account linking, and organization assumptions before the Agent Conversation experience is proven.
